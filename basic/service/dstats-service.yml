apiVersion: v1
kind: Service
metadata:
  name: dstats-service
  namespace: dstats
  labels:
    app: dstats
spec:
  selector:
    app: dstats
  ports:
  - protocol: TCP
    port: 80
    targetPort: 2743
    nodePort: 30080
    name: http
  type: NodePort